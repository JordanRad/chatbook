services:           
  user-management:
    container_name: user-management-api
    build: 
      context: ./
      dockerfile: Dockerfile.dev
    ports:
      - "8091:8091"
    volumes:
      - $PWD:/api
    depends_on:
      - sfit-db
    environment:
     - SFIT_HTTP_HOST=0.0.0.0
     - SFIT_HTTP_PORT=8090
     - SFIT_POSTGRES_HOST=user-management-db
     - SFIT_POSTGRES_PORT=5432
     - SFIT_POSTGRES_USER=um-dev
     - SFIT_POSTGRES_PASSWORD=123456
     - SFIT_POSTGRES_DB_NAME=um-dev-db
     - SFIT_JWT_SECRET=9r57294ftt76g8f3t7

  user-management-db:
    image: "postgres"
    restart: "always"
    ports:
      - "5433:5432"
    environment:
      POSTGRES_PASSWORD: "123456"
      POSTGRES_USER: "um-dev"
      POSTGRES_DB: "um-dev-db"